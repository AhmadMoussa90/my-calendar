import { Room, TimeSlot } from "../../models";

const Calendar: React.FC<{
  roomsItems: Room[];
  timeSlotsItems: TimeSlot[];
}> = () => {
  return <h2>My Calendar</h2>;
};

export default Calendar;
